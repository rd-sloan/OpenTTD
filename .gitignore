/.vs
/build*
CMakeSettings.json
docs/aidocs/*
docs/gamedocs/*
docs/source/*
/out
/vcpkg_installed

# ECS migration benchmark harness: keep bench.cfg, the runner and the README,
# but not the savegame fixtures or the generated reports.
/benchmark/out
/benchmark/saves
# Profiler captures. These reach tens or hundreds of megabytes once sampling is
# on, so they must never be committed.
/benchmark/manual-traces
